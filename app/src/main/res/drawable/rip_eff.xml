<!-- custom_ripple.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#80FFFFFF"> <!-- Change the color as needed -->
    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <solid android:color="@color/pink"/> <!-- Change the background color if needed -->
        </shape>
    </item>
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/> <!-- Change the mask color if needed -->
            <corners android:radius="8dp"/> <!-- Adjust the radius as required -->
        </shape>
    </item>
</ripple>
